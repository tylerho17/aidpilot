# ProgressBar

Rounded track with the signature blue→light-blue gradient fill that eases its width in. Used for FAFSA/onboarding completion and aid goals.

```jsx
<ProgressBar pct={68} />
<ProgressBar pct={68} label="FAFSA readiness" showValue />
```
`fill` overrides the gradient; `height`, `track`, `label`, and `showValue` are optional.
