# Select

Native `<select>` styled to match `TextField`, with the AidPilot chevron. Pass `options` as strings or `{value,label}`.

```jsx
<Select label="Year" options={["Freshman","Sophomore","Junior","Senior","Transfer","Graduate"]} />
<Select label="FAFSA status" options={["Yes","Not yet","I am not sure"]} />
```
