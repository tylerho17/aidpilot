# Checkbox

Custom checkbox row — a rounded box that fills brand-blue with a white check. Controlled.

```jsx
<Checkbox checked={on} onChange={() => setOn(!on)} label="Pell Grant" />
```
Use for multi-select lists (aid types, goals, scholarship interests).
