# SegmentedControl

Row of pill tabs (the marketing demo switcher). Active tab is solid brand-blue with a blue shadow; the rest are white outlines.

```jsx
<SegmentedControl value={tab} onChange={setTab}
  options={[{value:"protect",label:"Protect aid"},{value:"deadlines",label:"Catch deadlines"}]} />
```
